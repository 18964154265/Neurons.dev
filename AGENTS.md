# AGENTS.md - Atoms Demo

## 作用

本文件是项目长期有效的基础约束，用于防止上下文增长、会话切换或多人协作后偏离产品方向。

本文件只记录稳定且必须始终遵守的原则。具体需求、交互细节和验收标准写入 PRD；架构、数据模型、接口和技术实现写入 TRD。不要把易变细节继续堆入本文件。

## 指令优先级

按以下顺序处理冲突：

1. 用户当前明确提出的要求
2. 本文件中的长期约束
3. 已确认的 PRD
4. 已确认的 TRD
5. 现有代码和其他项目文档

如果用户的新要求会改变本文件中的长期约束，应先指出影响并确认，不得静默改变产品方向。

## 开始任何任务前

1. 完整阅读本文件。
2. 如果存在 PRD、TRD 或与当前任务相关的设计文档，先阅读再行动。
3. 检查现有代码、依赖、数据结构和未提交修改，不要只凭对话记忆推断项目状态。
4. 用一句话确认当前任务的目标和完成条件；小任务可以在内部完成，不必向用户重复。
5. 发现文档与代码不一致时，以证据说明差异，不得自行选择一个版本掩盖冲突。

长任务或上下文较长时，在继续工作前重新读取本文件及相关 PRD/TRD。不要依赖早期会话中的模糊记忆。

## 产品定位

本项目是一个受 Atoms 启发的 AI Native 应用构建工作区。

用户通过自然语言与 AI 协作，将想法逐步转化为可运行的网页应用。产品必须体现完整且连续的工作过程：理解需求、推进任务、生成或修改代码、查看运行反馈、预览结果、继续迭代、管理版本并发布。

它不是静态设计稿、纯聊天机器人，也不是一次性代码生成页面。

## 固定页面结构

工作区采用左右分割布局，不得未经用户确认改成三栏或多栏主布局。

### 顶部栏

顶部栏承载全局项目操作，至少包含：

- 项目
- 版本
- Publish

这些操作的具体交互由 PRD 定义，但在信息架构中必须始终可发现。

### 左侧：对话区

左侧是用户与 AI 协作的主要入口，用于：

- 输入需求和后续修改
- 展示用户消息与 AI/Agent 响应
- 展示真实的任务状态、需要用户处理的问题和失败反馈
- 支持围绕同一项目持续迭代

不得展示虚构的 Agent 行为、工具调用、构建日志或完成状态。

### 右侧：画布区

右侧是统一画布，通过 View 切换承载：

- 代码
- Terminal
- 预览

同一时间只展示当前选中的主 View。切换 View 时应保留项目状态、用户上下文和各 View 必要的内部状态，不得因为切换而丢失未完成工作。

代码、Terminal 和预览不是三个独立产品页面，而是同一个项目工作过程的不同观察方式。

## 核心产品原则

- 对话必须能够推动项目发生真实变化，而不是只返回说明文字。
- 代码、Terminal 和预览必须对应同一个项目版本，避免展示彼此不一致的状态。
- 所有长耗时操作都必须提供真实、可理解的状态和停止或恢复路径。
- 错误是正常产品状态。应保留用户输入，解释失败位置，并提供重试、修复或回退路径。
- 每次迭代都不得破坏最近一次可用结果；进行中的结果和已成功结果必须清楚区分。
- 版本、Remix 和 Publish 的含义必须保持稳定：版本用于追踪状态，Remix 用于从现有状态创建独立分支，Publish 用于生成或更新可访问结果。
- 用户刷新或重新进入项目后，关键项目数据和最近可用状态必须可以恢复。
- 所有可见控件必须真实可用。禁止提交无行为按钮、伪造进度、静态假数据冒充真实功能或仅为展示而存在的流程。

## PRD 与 TRD 的职责

### PRD 应负责

- 用户与使用场景
- 功能范围和优先级
- 完整用户流程
- 页面状态与交互规则
- Agent 行为的产品表现
- 版本、Remix、Publish 的详细规则
- 验收标准和非目标
- PRD不要与TRD耦合，即PRD里不要明文规定或者引导使用某种具体技术实现方式

### TRD 应负责

- 技术栈与依赖选择
- 系统架构和模块边界
- Agent/LLM 调用流程
- 数据模型与持久化方案
- API、事件和状态协议
- 代码执行与预览机制
- 安全、性能、测试和部署方案

编写 PRD 或 TRD 时，以本文件为边界，但不要机械复制本文件。对尚未确定的事项明确标记为待确认，不得擅自把假设写成既定事实。

实现代码时遵循已确认的 PRD 和 TRD。如果实现需要偏离文档，应先说明原因、影响和替代方案。

## 上下文连续性

- 一个项目只有一个当前事实来源；不要在多个文件中维护互相冲突的需求或技术决策。
- 不要从旧 Prompt、旧计划或废弃实现中恢复已经被新决策替代的内容。
- 修改既有行为前，先搜索调用方、测试、文档和相邻功能。
- 完成任务时说明实际完成内容、验证结果、未完成项和仍存在的风险。
- 不得声称未运行的测试已经通过，不得声称未验证的功能可用。
- 如果缺少关键信息但可以从仓库或文档中确认，应先自行检查；只有会实质改变产品方向或安全边界时才询问用户。

## 工程原则

- 优先建立可运行的纵向流程，再扩展次要能力。
- 保持模块职责清晰，避免把对话、项目状态、模型调用、执行环境和持久化逻辑混在单一组件中。
- 遵循仓库已确认的技术栈和约定，不得同时引入功能重叠的框架或状态管理方案。
- 添加依赖前检查现有依赖和 lockfile，并说明新增依赖解决的具体问题。
- 优先复用现有组件和模式；不要提前设计没有真实使用场景的通用抽象。
- 修复根本原因，不使用假成功、吞掉异常或仅遮盖错误的 UI 绕过方案。
- 保持改动聚焦，保留用户已有修改，不重构与当前任务无关的代码。
- 所有外部输入、模型输出和持久化数据都必须在系统边界进行类型与结构校验。
- 做好职责分离、数据解耦、模块复用。
- 不要写过多的兜底代码。

## AI 与运行安全

- Prompt、模型输出、生成代码、文件名、日志和外部内容均视为不可信数据。
- 模型生成的文本不能被宿主应用当作系统指令执行。
- 生成代码只能在 TRD 明确批准的隔离环境中运行，禁止直接在应用服务端、Shell、`eval`、`new Function`、子进程或未隔离运行时中执行。
- 禁止根据模型输出安装任意依赖、执行任意命令或访问未授权资源。
- 文件路径、文件类型、依赖、请求大小、输出大小、执行时间和重试次数必须有明确边界。
- 密钥只能存在于服务端或批准的 Secret 管理系统中，禁止写入代码、日志、对话、生成文件或浏览器可访问的数据。
- 每个数据写操作都必须执行身份、所有权和权限检查；客户端过滤不等于权限控制。
- 失败时应安全关闭，不得为了让流程看起来成功而绕过校验、权限或隔离措施。

## UX 与可访问性底线

- 核心工作区优先于营销页面和装饰性内容。
- 状态、主操作、错误和恢复方式必须清晰可见。
- 保留键盘操作、合理焦点、可读对比度、表单标签和响应式行为。
- 不使用无意义动画或视觉效果掩盖状态变化。
- 桌面端和移动端都必须可用；具体断点和适配规则由 PRD 定义。
- 用户看到的文案、日志和状态必须准确，不得用技术术语堆砌来代替清晰解释。

## 测试与验收底线
- 对于后端的接口，创建完成后，生成mock数据进行测试，将mock数据写到"/test_cases/"下，每个接口创建一个json，每个json里，每条test_case的第一个键是本条test描述，后面跟上具体的数据。
- 每项功能至少验证成功路径、失败路径和恢复路径。
- 核心流程必须有端到端测试；边界校验和关键状态转换应有确定性测试。
- 自动化测试不得依赖收费或不确定的真实模型输出，应在明确的测试边界使用可复现 Fixture 或 Adapter。
- 测试替身只能用于测试，不得让生产流程以 Mock 冒充真实能力。
- 交付前至少执行仓库约定的 lint、类型检查、测试和构建。
- 只有实际执行成功的检查才能记录为通过；跳过或失败的检查必须如实报告。
- 发布后必须在全新会话中验证真实访问、核心交互、状态恢复和错误处理。

## 完成标准

任务只有在以下条件同时满足时才算完成：

- 行为符合本文件及当前确认的 PRD/TRD
- 用户可见功能真实可用
- 关键状态可以持久化和恢复
- 失败不会破坏最近一次可用结果
- 安全边界没有被绕过
- 相关检查已经执行并如实记录
- 没有隐藏未完成的关键路径、伪造结果或遗留阻塞问题

除非用户明确要求，否则不要 Commit、Push、Deploy、修改付费服务、轮换密钥或执行破坏性数据操作。
