# Neurons 产品需求文档（PRD）

> 文档状态：初稿，待评审
>
> 版本：v0.1
>
> 更新日期：2026-08-29
> 适用范围：首个可用版本的产品定义，不包含技术实现方案

## 1. 产品概述

Neurons 是一款面向大众用户的 Vibe Coding 网页应用。用户不需要先理解工程结构或开发工具，只需通过自然语言描述想法，即可与一个或多个预定义 Agent 协作，将想法逐步转化为可运行、可预览、可继续修改的网页应用。

产品的核心体验不是一次性生成代码，而是在同一个项目内连续完成需求沟通、任务执行、代码修改、终端操作、结果预览、过程追踪、版本管理与发布。

首版包含两个核心页面：

| 核心页面   | 用户任务                                     | 主要内容                                                        |
| ---------- | -------------------------------------------- | --------------------------------------------------------------- |
| Dashboard  | 开始新项目、找到已有项目、恢复最近工作上下文 | 项目列表、历史入口、新建对话栏                                  |
| 项目工作区 | 与 Agent 持续协作并检查真实执行结果          | 左侧 Agent 对话；右侧 Editor、Terminal、Web Preview、Trace 画布 |

## 2. 产品目标

### 2.1 用户目标

- 用自然语言快速开始一个网页应用项目。
- 清楚知道当前由谁执行、正在做什么、是否成功以及下一步是什么。
- 在不理解全部技术细节的情况下查看和验证实际结果。
- 在需要时查看代码、终端输出和完整执行轨迹。
- 围绕同一个项目持续对话和迭代，并能恢复历史状态。

### 2.2 业务目标

- 降低大众用户从想法到可运行网页应用的门槛。
- 通过 Engineer Mode 与 Team Mode 同时覆盖简单任务和复杂任务。
- 建立可信、透明且可持续迭代的 Agent 工作体验。
- 为后续扩展更多画布模式、Agent 能力和协作控制预留产品空间。

### 2.3 首版成功信号

- 用户能够从 Dashboard 发起对话并自动获得一个可持续编辑的项目。
- 用户能够完成“提出需求—Agent 执行—查看过程—预览结果—继续修改”的完整闭环。
- 用户能够理解 Engineer Mode 与 Team Mode 的区别并正确使用。
- 用户能够在摘要信息与完整 Trace 之间顺畅切换。
- 页面刷新或重新进入后，项目、对话和最近可用结果仍可恢复。

具体量化指标（如项目创建成功率、首次预览时间、任务成功率和次日留存）待产品验证阶段补充。

## 3. 目标用户与典型场景

### 3.1 目标用户

- 没有编程经验，但希望将创意做成网页应用的大众用户。
- 有基础技术认知，希望快速完成原型的产品经理、设计师和创业者。
- 希望减少重复开发工作、快速验证想法的开发者。

### 3.2 典型场景

- 从一句描述创建活动页、作品集、数据看板或简单工具。
- 上传或补充需求后，让 Agent 修改已有项目。
- 在 Agent 编写代码或执行命令时实时跟随其操作。
- 由多个预定义 Agent 分工完成较复杂的项目任务。
- 从摘要中发现关键节点，并在 Trace 中检查完整上下文。
- 回到历史项目继续对话、查看版本或发布结果。

## 4. 核心概念

### 4.0 账户与会话

- 用户可以使用邮箱和密码注册、登录及重置密码。
- 注册是否需要邮箱确认由当前环境的账户安全配置决定，界面必须明确提示下一步。
- 同一邮箱对应的同一账户再次登录后，应恢复其原有项目、对话、运行记录和版本。
- 登录状态应在刷新和重新打开浏览器后恢复，并在会话可刷新期间自动续期；主动退出、会话被撤销或长期失效后要求重新登录。
- 已经通过邮箱登录链接创建过账户的用户，可以通过“忘记密码”流程为原账户设置密码，不得因此丢失原有项目。

### 4.1 项目

项目是用户工作的基本容器，包含对话、任务、Agent 分配、代码与资源、运行状态、预览结果、Trace、版本和发布状态。

Dashboard 中发起的每个新对话都会自动创建一个新项目；新项目创建成功后，用户直接进入该项目的工作区。首次用户消息作为项目的初始需求，并用于生成默认项目名称。名称可在后续修改。

### 4.2 对话

对话是用户驱动项目变化的主要入口。用户消息、Agent 回复、任务状态、压缩后的 Thought 与 Tool Call、问题、错误和恢复动作都在同一时间线中展示。

对话中展示的状态必须来自真实执行，不得用静态内容或模拟进度冒充 Agent 行为。

### 4.3 Agent

Agent 是系统预先定义且可跨项目复用的执行角色。每个 Agent 至少包含以下展示信息：

| 信息项       | 用户需要理解的内容                         |
| ------------ | ------------------------------------------ |
| 名称与头像   | 当前角色是谁                               |
| 能力描述     | 该角色适合处理什么任务                     |
| Tools 列表   | 该角色被允许执行哪些操作                   |
| 当前状态     | 正在执行、等待、失败、完成或其他真实状态   |
| 项目分配状态 | 是否已加入当前项目，与当前运行状态分开表达 |

不支持用户创建、删除或编辑 Agent。首版固定提供以下五个可跨项目复用的角色：

| Agent | 角色                    | 主要职责                                         | 模式约束                       |
| ----- | ----------------------- | ------------------------------------------------ | ------------------------------ |
| Mike  | Team Lead               | 理解目标、拆分任务、协调团队并汇总交付           | Team Mode 的默认调度负责人     |
| Emma  | Product & Research      | 澄清需求、研究资料并形成可实现、可验收的产品定义 | 按任务需要参与 Team Mode       |
| Bob   | System Architect        | 负责架构、数据、接口、安全和实现顺序设计         | 按任务需要参与 Team Mode       |
| Alex  | Full-stack Engineer     | 负责实际代码实现、运行、修复和基础验证           | Engineer Mode 只启用 Alex      |
| David | Quality & Data Engineer | 负责测试、数据检查、Trace 分析和交付验收         | 按任务需要参与验证与发布前检查 |

用户指定 Agent 时严格按其选择执行，不得静默替换或增加成员。同一文件同一时间只有一个写入负责人；所有 Agent 只展示安全执行摘要，不展示隐藏思维链。

### 4.4 项目历史与版本

| 概念     | 回答的问题                 | 主要内容                                 | 核心操作         |
| -------- | -------------------------- | ---------------------------------------- | ---------------- |
| 项目历史 | 这个项目发生过什么？       | 对话、任务执行和关键事件时间线           | 浏览、定位和追溯 |
| 版本     | 项目在某个时点是什么状态？ | 可恢复、可比较的代码、资源和运行结果状态 | 查看、比较和恢复 |

二者应在用户心智上清楚区分，不能用同一列表或同一状态概念混合呈现。

## 5. 信息架构

```text
Dashboard
├── 历史对话 / 项目导航
├── 项目列表
└── 新建对话输入区
    └── 发送首条消息 → 自动创建项目 → 进入项目工作区

项目工作区
├── 全局项目操作：项目 / 版本 / Publish
├── 左侧对话区（约 1/3）
│   ├── 顶部栏：首页 / 项目信息 / 项目历史
│   ├── 对话与执行过程
│   └── 输入区：模式 / 调度方式 / 消息输入 / 执行控制
└── 右侧统一画布（约 2/3）
    ├── 画布顶栏：View / 跟随 / Agent 列表 / 上下文操作
    ├── Editor
    ├── Terminal
    ├── Web Preview
    └── Trace
```

桌面端以约 1:2 的左右分栏为默认布局；分栏的最小宽度、是否允许拖动和具体响应式断点待视觉与可用性验证后确定。

## 6. 核心用户流程

### 6.1 创建新项目

1. 用户进入 Dashboard。
2. 用户在对话栏中输入初始需求并发送。
3. 系统立即保留用户输入并进入创建状态，避免重复提交。
4. 系统自动创建项目，将首条消息写入项目对话。
5. 创建成功后进入项目工作区，Agent 开始理解和处理需求。
6. 如果创建失败，用户仍能看到并编辑原输入，可重试，不产生不可识别的空项目或重复项目。

### 6.2 Engineer Mode 执行

1. 用户选择 Engineer Mode，或沿用项目当前模式。
2. 系统明确显示本次由单个 Engineer Agent 执行。
3. 用户发送需求。
4. Agent 在对话区持续汇报真实状态，并展示压缩后的执行节点。
5. Agent 修改代码或执行终端命令时，画布默认跟随其当前操作。
6. 任务结束后显示结果、验证状态、失败信息或下一步建议。

### 6.3 Team Mode 执行

1. 用户选择 Team Mode。
2. 用户选择默认调度，或明确指定本次参与的 Agent。
3. 默认调度时，系统根据任务选择所需的预定义 Agent，并在执行前或分配发生时清楚显示选择结果。
4. 用户指定时，系统仅在所选 Agent 的能力与权限范围内调度；如无法完成，应在执行前解释限制并给出调整入口。
5. 多个 Agent 的分工、当前负责人、交接和任务状态在对话区可识别。
6. 画布顶栏同步显示哪些 Agent 已被分配到当前项目。
7. 执行完成后，以项目整体结果收束，避免只呈现彼此割裂的 Agent 消息。

### 6.4 查看完整 Trace

1. 用户在对话时间线中看到压缩后的 Thought 或 Tool Call 节点。
2. 关键节点提供“查看详情”入口；普通节点也应具备一致的可展开规则，具体层级待确认。
3. 用户点击后，右侧画布自动切换到 Trace View。
4. Trace 定位到对应事件，并显示该节点允许用户查看的完整内容及上下文。
5. 用户关闭详情或切换 View 后，可回到之前的画布状态；对话和任务执行不中断。

### 6.5 跟随 Agent 操作

1. 进入项目工作区时，“跟随”默认为开启。
2. Agent 编辑代码时，画布切换或定位到相关 Editor 内容。
3. Agent 执行命令时，画布切换或定位到相关 Terminal 会话。
4. 用户手动关闭跟随后，画布保持用户当前 View 和位置，Agent 继续后台执行。
5. 跟随关闭期间，界面以非打断方式提示新的 Agent 活动，用户可一键恢复跟随并跳到最新操作。
6. 用户手动切换 View 或检查旧内容时，不应被 Agent 的下一次事件立即抢走焦点；系统应明确提示跟随状态，并采用一致规则处理本次手动操作。该规则待交互评审确认。

## 7. 页面与功能需求

### 7.1 Dashboard

#### 7.1.1 页面目标

让用户快速开始新项目、找到已有项目并恢复最近的工作上下文。

#### 7.1.2 页面组成

| 区域       | 核心内容与操作                                                           | 边界与待确认项                                                                               |
| ---------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| 历史导航   | 展示用户有权访问的历史对话或项目；按最近更新时间排序；点击后恢复最近状态 | 首版默认“一个项目一条连续主对话”；是否支持一个项目多条独立会话待确认                         |
| 项目列表   | 展示项目名称、最近更新时间、最近状态和必要预览信息；支持进入项目         | 至少区分执行中、需要处理、失败、已有可用结果和已发布；搜索、筛选、归档和删除范围见待确认项   |
| 新建对话栏 | 多行输入、发送、输入保留和创建中状态；失败后可编辑并重试                 | 服务于创建项目而非普通闲聊；空内容不可发送；模式是在首条消息前选择还是创建后使用默认值待确认 |

#### 7.1.3 页面状态

| 页面状态   | 页面表现                                   | 用户可执行操作       |
| ---------- | ------------------------------------------ | -------------------- |
| 首次使用   | 突出新建对话，并用简短示例帮助用户描述目标 | 输入需求并创建项目   |
| 有历史项目 | 展示最近项目及继续入口                     | 继续项目或创建新项目 |
| 加载中     | 显示真实加载状态，不显示虚构项目           | 等待；必要时可取消   |
| 空结果     | 解释没有可展示内容的原因                   | 创建新项目           |
| 加载失败   | 保留仍可用的页面区域并说明失败             | 重试                 |

### 7.2 项目工作区

#### 7.2.1 整体布局

- 左侧约 1/3 为对话区，右侧约 2/3 为统一画布。
- 两侧属于同一个项目上下文，任何 View 切换都不得重置对话、任务或项目状态。
- “项目 / 版本 / Publish”作为全局项目操作必须始终可发现；具体放在跨区域的全局顶部栏，还是合并进画布顶栏，待视觉设计确认。

#### 7.2.2 左侧顶部栏

- 左侧为首页图标按钮。
  - 点击后返回 Dashboard。
  - 若当前有任务正在执行，不默认停止任务；应在离开前清楚说明后台状态，或按最终确定的离开规则处理。
  - 图标需有可访问名称和悬停提示。
- 中部可显示项目名称和必要状态；具体样式待设计。
- 右侧为项目历史入口。
  - 打开当前项目的历史记录。
  - 至少能够看到消息、任务、关键事件及其时间。
  - 可从历史定位回对应对话节点或版本。

#### 7.2.3 对话时间线

对话区按时间顺序展示：

- 用户消息。
- Agent 的面向用户回复。
- 当前执行 Agent 与任务状态。
- 压缩后的 Thought 摘要。
- 压缩后的 Tool Call 摘要。
- Agent 分配、交接和完成等关键事件。
- 需要用户确认、补充或处理的问题。
- 错误、重试、停止、恢复和回退状态。

压缩节点的展示要求：

- 默认只展示对用户有判断价值的摘要，避免原始日志淹没对话。
- 明确标识节点类型、所属 Agent、状态和时间顺序。
- 关键节点提供“查看详情”，并在 Trace View 中定位完整内容。
- 进行中、成功、失败、取消和等待用户的视觉状态必须可区分，不能只依靠颜色。
- 不展示敏感信息、密钥或不应暴露的内部数据。
- “完整 Thought”的可见范围需符合安全和模型能力边界；产品保证展示可审计的执行详情，不承诺披露模型私有推理。最终文案与展示字段待确认。

#### 7.2.4 对话输入区

- 支持多行自然语言输入与发送。
- 清楚显示当前是 Engineer Mode 还是 Team Mode。
- Team Mode 下提供“默认调度 / 用户指定”选择。
- 用户指定调度时，可从预定义 Agent 中选择本次任务的参与者。
- 无论 Run 处于何种状态，输入框都允许编辑并保留下一条 Prompt 草稿。
- 首版不在活跃 Run 期间并发发送或自动排队；发送按钮暂时不可用，并明确提示草稿会在当前任务结束后发送。草稿不得被 Run 状态刷新、失败或取消清空。
- 任务耗时较长时提供真实进度和停止或恢复路径。
- 输入失败或网络异常时保留用户内容。

### 7.3 统一画布

#### 7.3.1 画布顶栏

顶栏包含以下能力：

| 能力区         | 默认表现与核心行为                                                          | 状态与边界                                                                                 |
| -------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| View 切换      | 提供 Editor、Terminal、Web Preview、Trace；切换时保留各 View 必要状态       | 新增 View 不应改变用户对现有切换方式的认知                                                 |
| 跟随控制       | 默认开启；开启时跟随当前活跃 Agent 的代码与终端操作；关闭时保留用户当前位置 | 必须明确显示开关状态；控制区应可扩展，不能把顶栏结构硬编码为单一跟随按钮                   |
| Agent 头像列表 | 展示全部预定义 Agent；悬停或键盘聚焦时显示名称、描述、Tools 和分配状态      | 已分配与未分配不能只依赖明暗；运行状态与分配状态分开表达；头像点击后的筛选或调度能力待确认 |
| 项目操作       | 项目、版本和 Publish 在工作区内持续可发现                                   | 不因 View 切换而消失或失效                                                                 |

顶栏控件应由可扩展的能力集合驱动产品呈现，新增 View、控制项或 Agent 时，不应要求改变用户对现有控件的基本认知。

#### 7.3.2 Editor View

- 提供接近 VS Code 心智模型的代码查看与编辑体验。
- 至少支持文件浏览、打开文件、代码编辑、未保存状态和当前 Agent 操作定位。
- Agent 编辑时，在跟随开启状态下定位到对应文件和操作位置，并明确显示操作主体。
- 用户主动编辑与 Agent 同时编辑时，必须有清晰的冲突和状态规则；首版具体协作策略待确认。
- View 切换后保留已打开文件、光标或滚动等必要状态。
- Agent 的进行中修改与最近一次可用版本应可区分。

#### 7.3.3 Terminal View

- 展示与当前项目相关的真实终端会话和命令输出。
- 标明命令由哪个 Agent 或用户触发。
- 跟随开启时定位到当前活跃命令及最新输出。
- 对运行中、成功、失败、已停止和超时状态做清晰区分。
- 长输出可折叠或截断，但必须提供查看所需上下文的方式。
- 用户是否可直接输入任意命令，以及首版权限范围，待确认。

#### 7.3.4 Web Preview View

- 展示当前项目的可运行网页结果。
- 清楚区分正在构建、最新成功结果和当前失败尝试。
- 新任务失败时保留最近一次可用预览，不用空白或错误结果覆盖。
- 提供刷新、在必要范围内重新加载和查看预览错误的入口。
- 预览对应的项目版本必须可识别，避免与 Editor 或 Terminal 状态不一致。
- 响应式尺寸切换、独立打开和地址导航是否进入首版，待确认。

#### 7.3.5 Trace View

Trace 用于查看 Agent 执行的可审计详情，既可从 View 切换进入，也可由对话中的“查看详情”直接定位。

至少展示：

- 事件时间与顺序。
- 所属任务和 Agent。
- 事件类型与状态。
- 对用户可见的执行说明或 Thought 摘要/详情。
- Tool Call 的工具名称、输入摘要、结果摘要、耗时和错误信息。
- 与代码文件、终端命令、预览或版本的关联。
- 多 Agent 的分配、交接和依赖关系。

Trace 交互要求：

- 从对话进入时高亮并定位对应事件。
- 支持返回先前 View 和浏览位置。
- 长内容分层展示，默认保持可读。
- 敏感数据必须隐藏或脱敏。
- 进行中的事件可持续更新，但不能伪造已完成状态。
- 首版是否提供按 Agent、事件类型或状态筛选，待确认。

## 8. 模式与 Agent 调度规则

| 对比维度     | Engineer Mode                          | Team Mode                                                  |
| ------------ | -------------------------------------- | ---------------------------------------------------------- |
| 执行主体     | 仅 Alex，一个主执行 Agent              | Mike 负责调度一个或多个预定义 Agent                        |
| 适用任务     | 范围明确、复杂度较低或希望减少协作开销 | 需要产品、架构、工程、测试等多领域协作的任务               |
| 调度方式     | 固定单 Agent                           | 默认调度或用户指定                                         |
| 用户可见信息 | 当前执行者、真实任务状态和结果         | 分工、当前负责人、成员状态、关键交接和项目级汇总           |
| 关键限制     | 不得显示虚假的多 Agent 协作状态        | 只能使用预定义角色；用户指定时不得静默替换或增加其他 Agent |

### 8.1 Engineer Mode

- 单 Agent 模式。
- 一次任务只有一个主执行 Agent。
- 适合范围明确、复杂度较低或用户希望减少协作开销的任务。
- 界面不得出现多 Agent 正在协作的误导性状态。

### 8.2 Team Mode

- 多 Agent 协作模式。
- Agent 全部来自系统预定义集合，并跨项目复用角色定义。
- 支持默认调度和用户指定两种方式。
- 必须展示 Agent 的分工、状态和关键交接。
- 最终结果以项目任务为单位统一汇总。

### 8.3 默认调度

- 系统根据任务需求决定所需 Agent。
- 仅分配完成任务所需的 Agent，避免无意义地全员参与。
- 新增分配、撤销分配或任务交接时，应在对话和头像状态中同步反映。
- 用户可查看选择了哪些 Agent 及其职责摘要。

### 8.4 用户指定调度

- 用户从预定义 Agent 中选择参与本次任务的成员。
- 系统在执行前校验所选 Agent 是否能覆盖任务需要。
- 能力不足时不静默改派；应解释原因，并让用户确认是否补充 Agent、改用默认调度或继续执行受限方案。
- 用户指定的是任务级参与者还是项目级长期成员，待确认；头像亮暗首版按“已分配到当前项目”呈现。

### 8.5 模式切换

- 模式切换不应创建新项目或丢失历史。
- 已在执行的任务不可被无提示地改变调度方式。
- 切换是影响下一条任务，还是可在当前任务中动态生效，待确认；首版建议仅影响下一次尚未开始的任务。

## 9. 状态、异常与恢复

### 9.1 项目状态

| 状态层级     | 状态         | 用户需要理解的含义                         |
| ------------ | ------------ | ------------------------------------------ |
| 当前任务状态 | 就绪         | 当前没有正在执行或等待处理的任务           |
| 当前任务状态 | 执行中       | Agent 正在处理本次任务                     |
| 当前任务状态 | 等待用户     | 继续执行前需要用户确认、选择或补充信息     |
| 当前任务状态 | 执行失败     | 本次任务未完成，需要修复、重试或回退       |
| 当前任务状态 | 已停止       | 用户或系统已终止本次任务                   |
| 项目结果状态 | 已有可用结果 | 项目存在最近一次可继续使用或预览的成功结果 |
| 项目发布状态 | 已发布       | 存在用户可访问的已发布结果                 |

状态可组合时，应明确“当前任务状态”与“项目最近可用状态”的区别。

### 9.2 异常处理原则

| 异常场景        | 必须保留的内容                                     | 用户反馈与恢复动作                             |
| --------------- | -------------------------------------------------- | ---------------------------------------------- |
| 创建失败        | 原始输入                                           | 允许编辑并重试，同时避免产生重复项目           |
| Agent 执行失败  | 已完成操作、失败节点、影响范围                     | 提供重试、修复、调整或回退入口                 |
| Tool Call 失败  | 对话中的安全摘要和 Trace 中可公开的错误详情        | 标明工具、触发位置和可执行的下一步             |
| 预览失败        | 最近一次成功预览                                   | 展示当前失败尝试，不用失败结果覆盖最近可用结果 |
| 页面刷新或断线  | 项目、对话、任务状态、Agent 分配和最近可用画布状态 | 自动恢复；恢复失败时说明缺失范围并提供重试     |
| 部分 Agent 失败 | 其他 Agent 的真实运行状态和已完成产物              | 说明是否影响整体任务，以及其他 Agent 是否继续  |
| 用户停止任务    | 已完成和未完成操作                                 | 明确停止结果并保留恢复或重新发起路径           |

## 10. 版本、Remix 与 Publish

以下能力属于产品长期核心，但本轮描述尚未给出完整规则，首版先保留稳定入口与基础定义，详细交互待后续 PRD 迭代补充。

| 能力    | 产品定义                                           | 首版要求与待确认项                                                       |
| ------- | -------------------------------------------------- | ------------------------------------------------------------------------ |
| 版本    | 用于追踪、识别和恢复项目状态                       | 当前画布、预览和发布结果对应版本应可识别；新任务不得破坏最近一次成功版本 |
| Remix   | 从现有项目状态创建独立分支，不覆盖原项目           | 是否进入首版及入口位置待确认                                             |
| Publish | 将最近可发布的成功结果生成或更新为用户可访问的结果 | 发布中、成功、失败和过期状态必须真实；域名、权限、更新和撤回规则待确认   |

## 11. 响应式与可访问性

### 11.1 桌面端

- 不考虑，后续也不会开发，减少不必要的拓展性预留代码。

### 11.2 窄屏与移动端

- 不考虑，后续也不会开发，减少不必要的拓展性预留代码。

### 11.3 可访问性

- 所有图标按钮具有可读名称和键盘焦点。
- View、模式、跟随和 Agent 分配状态不能只用颜色表达。
- 头像悬停信息同时支持键盘聚焦和触屏访问。
- 状态更新应对辅助技术可感知，同时避免频繁播报造成干扰。
- 文本、代码和状态具有足够对比度。

## 12. 首版范围与优先级

### P0：首个可用闭环

- Dashboard 的项目列表、历史入口和新建对话栏。
- 发送首条消息后自动创建项目并进入工作区。
- 项目工作区 1:2 左右布局。
- 连续对话与真实任务状态。
- Engineer Mode。
- Team Mode 的默认调度和用户指定调度。
- 预定义 Agent 展示、分配状态和详情浮层。
- Editor、Terminal、Web Preview、Trace 四个 View。
- 默认开启的跟随能力及关闭/恢复跟随。
- 压缩 Thought 与 Tool Call 节点，以及从关键节点进入 Trace。
- 项目历史入口。
- 最近可用结果保护、错误反馈和刷新恢复。
- 项目、版本和 Publish 的可发现入口；其中版本和 Publish 的完整能力可按后续确认拆分交付。

### P1：增强效率与可控性

- Dashboard 搜索、筛选和归档。
- Trace 多条件筛选。
- Agent 头像点击后的跟随或过滤能力。
- 更完整的版本查看、比较与恢复。
- Web Preview 响应式尺寸和独立打开。
- 项目名称编辑与更多管理操作。

### P2：后续扩展

- Remix。
- 更多画布 View 与顶栏控制能力。
- 更丰富的项目协作与分享。
- 用户可配置但不创建 Agent 的偏好设置。

优先级将在首轮反馈后重新确认。

## 13. 非目标

首版明确不包含：

- 用户自行创建、删除、编辑或上传 Agent。
- 以静态演示数据伪装 Agent、Tool Call、终端或构建执行。
- 将 Editor、Terminal、Preview、Trace 做成互相割裂且状态不一致的独立产品。
- 无恢复路径的一次性代码生成体验。

多人共同编辑同一项目、插件市场、原生移动应用、复杂企业权限和计费体系是否属于后续方向，本轮不做结论。

## 14. 核心验收标准

### 14.1 Dashboard 与项目创建

- 用户发送有效首条消息后，系统只创建一个对应项目，并自动进入该项目工作区。
- 项目创建失败时，原输入不丢失，用户可以重试。
- 用户从 Dashboard 可进入历史项目，并恢复其对话和最近状态。

### 14.2 对话与模式

- Engineer Mode 的一次任务仅显示一个执行 Agent。
- Team Mode 支持默认调度和用户指定，参与 Agent 与实际执行一致。
- 对话能准确展示执行中、等待用户、成功、失败和停止状态。
- Agent 的 Thought 与 Tool Call 以压缩节点展示，关键节点可打开对应 Trace。

### 14.3 画布与 Trace

- 用户可在四个 View 间切换，项目任务和各 View 必要状态不丢失。
- 点击对话中的详情后，Trace 自动定位到正确事件。
- Editor、Terminal、Preview 和 Trace 展示的是同一项目上下文。
- 新执行失败不会覆盖最近一次成功预览。

### 14.4 跟随与 Agent 列表

- 首次进入工作区时跟随默认开启。
- Agent 编辑代码或执行命令时，开启跟随可定位到相应操作。
- 关闭跟随后，Agent 活动不强制改变用户当前 View，并提供恢复到最新活动的入口。
- 顶栏展示全部预定义 Agent，已分配与未分配状态清晰可辨且不只依赖颜色。
- 鼠标悬停、键盘聚焦或触屏操作可以查看 Agent 描述和 Tools。

### 14.5 恢复与可信度

- 页面刷新或重新进入后，项目、对话、任务状态、Agent 分配和最近可用结果可恢复。
- 所有用户可见按钮均有真实行为。
- 不显示虚构日志、伪造进度或与真实执行不一致的完成状态。

## 15. 待确认问题

以下问题不会阻止本 PRD 作为首版讨论稿，但会影响下一版范围和交互细节：

1. 已确认的 Agent 主要能力分别映射到哪些具体可执行 Tool Key、风险等级和审批策略？
2. Dashboard 是分别展示“项目”和“对话历史”，还是以项目列表统一承载历史？
3. 一个项目是否只有一条连续主对话，还是允许创建多条独立会话？
4. 新建项目时默认使用 Engineer Mode 还是 Team Mode？用户是否在首条消息前选择？
5. 用户指定 Agent 是仅作用于下一次任务，还是长期改变项目成员？
6. 头像点击是否需要支持指定当前跟随 Agent、筛选 Trace 或直接调整分配？
7. 用户手动切换 View 时，是否自动暂停跟随，还是短暂保持用户控制后继续跟随？
8. “完整 Thought”最终允许展示哪些内容？建议对外定义为可审计的执行说明与工具详情，而非模型私有推理原文。
9. 用户能否直接编辑代码和输入终端命令？与 Agent 并发操作时如何处理冲突？
10. 项目历史的首版范围是否包含版本创建、比较和恢复？
11. Publish、Remix、项目删除、归档、分享分别何时进入范围？
12. Agent 执行期间离开项目或关闭页面时，任务应继续、暂停还是提示用户选择？
13. 首版对上传文件、图片、外部链接和项目导入的支持范围是什么？

## 16. 后续文档计划

本 PRD 经反馈确认后，应继续补充：

- 页面线框与关键交互稿。
- Agent 可执行 Tool、风险等级和审批策略清单。
- 版本、Remix、Publish 的详细产品规则。
- 状态文案与错误恢复矩阵。
- 量化产品指标。
- 与已确认 PRD 对齐、但独立维护的技术需求文档（TRD）。
